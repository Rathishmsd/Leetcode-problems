int maxSubArray(int* nums, int numsSize) {

	int cursum = nums[0];
	int max = nums[0];
	for(int i=1; i<numsSize; i++)
	{
		if(cursum<0)
		{
			cursum=0;
		}
		cursum = cursum + nums[i];
		if(max<cursum)
		{
			max=cursum;
		}
	}
	return max;
}
